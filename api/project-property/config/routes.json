{
  "routes": [
    {
      "method": "GET",
      "path": "/project-properties",
      "handler": "project-property.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/project-properties/count",
      "handler": "project-property.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/project-properties/:id",
      "handler": "project-property.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/project-properties",
      "handler": "project-property.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/project-properties/:id",
      "handler": "project-property.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/project-properties/:id",
      "handler": "project-property.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
